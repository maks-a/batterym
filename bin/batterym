#!/usr/bin/python
import sys
from os.path import abspath, dirname, join

try:
    import batterym
except ImportError:
    sys.path.append(abspath(join(dirname(__file__), '..')))

from batterym import main
main.run()
